<h1>Create New Activity</h1>

<form action="/activities" method="POST">
  <div>
    <label for="name">Name</label>
    <input type="text" name="name" value="{{newActivity.name}}" />
  </div>
  <div>
    <label for="description">Description</label>
    <input type="text" name="description" value="{{newActivity.description}}" />
  </div>
  <div>
    <label for="location">Location</label>
    <input type="location" name="location" value="{{newActivity.location}}"/>
  </div>
  <div>
    <label for="supplies">Supplies</label>
    <input type="text" name="supplies" value="{{newActivity.supplies}}" />
  </div>
  <div>
    <label for="contact">Contact</label>
    <input type="text" name="contact" value="{{newActivity.contact}}" />
  </div>
    <div>
    <label for="hours">Hours</label>
    <input type="text" name="hours" value="{{newActivity.hours}}" />
  </div>
  <button type="submit">Creates Activity</button>
</form>